// ملف الألوان الثابتة
import 'dart:ui';

class AppColors {
  // الألوان الأساسية
  static const Color primary = Color(0xFF7C3AED);
  static const Color primaryDark = Color(0xFF5B21B6);
  static const Color primaryLight = Color(0xFF8B5CF6);

  // ألوان التدرج
  static const Color gradientStart = Color(0xFF7C3AED);
  static const Color gradientEnd = Color(0xFF6366F1);

  // ألوان النصوص
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Color(0xFF9CA3AF);
  static const Color textWhite = Color(0xFFFFFFFF);

  // ألوان الخلفيات
  static const Color background = Color(0xFFF9FAFB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF111827);

  // ألوان الحالات
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // ألوان الحدود والتظليل
  static const Color border = Color(0xFFE5E7EB);
  static const Color shadow = Color(0x0D000000);

  // ألوان الأيقونات
  static const Color iconPrimary = Color(0xFF6B7280);
  static const Color iconActive = Color(0xFF7C3AED);

  // ألوان القائمة الجانبية
  static const Color sideMenuBg = Color(0xFFFFFFFF);
  static const Color sideMenuHeader = Color(0xFF7C3AED);
  static const Color menuItemHover = Color(0xFFF3F4F6);
  static const Color menuItemActive = Color(0xFFEDE9FE);

  // ألوان AppBar
  static const Color appBarBg = Color(0xFFFFFFFF);
  static const Color appBarShadow = Color(0x1A000000);
}
